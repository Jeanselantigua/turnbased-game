package com.battlesim.engine;

import com.battlesim.model.Character;
import com.battlesim.model.Move;

/**
 * Resolves a single turn between two Characters: decides who acts
 * first (priority, then speed) and applies the result.
 * TODO: implement turn order + applying the move.
 */
public class TurnResolver {

    public void resolveTurn(Character first, Move firstMove, Character second, Move secondMove) {
        // TODO: implement
    }
}
