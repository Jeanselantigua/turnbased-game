package com.battlesim.progress;

import com.battlesim.model.Character;

/** Leaves points unspent so the player can allocate them at camp. */
public final class BankStatAllocator implements StatAllocator {

    @Override
    public void allocate(Character character) {
    }
}
